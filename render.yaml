services:
  - type: web
    name: legacymind-alerts
    env: python
    buildCommand: ""
    startCommand: "gunicorn main:app"
    envVars:
      - key: BOT_TOKEN
        value: YOUR_BOT_TOKEN
      - key: CHAT_ID
        value: YOUR_CHAT_ID
